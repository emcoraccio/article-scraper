
{{#each this.note}}

<div class="col s12 center note">
  <h5>{{title}} <span><i data-note-id="{{_id}}" class="delNote right material-icons">delete</i></span></h5>
  <p class="noteBody">{{body}}</p>
  <hr>
</div>

{{/each}}